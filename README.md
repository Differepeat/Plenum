# Plenum
An experimental grid editor that can recursively convert a cell into a 7x7 grid, with the user being able to navigate through these potentially infinite layers. Was made to demonstrate a basic, constrained take on a post-structuralist, infinitely specific approach towards language.
